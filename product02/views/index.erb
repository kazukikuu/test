<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="UTF-8">
	<title>Musicsearch</title>
</head>
<body>
 <h1>最近の投稿</h1>
<% @posts.each do |t| %>
		<div>
		<img src="<%= t.image %>">
		<p><%= t.artist %></p>
		<p><%= t.album %></p>
		<p><%= t.track %></p>
		<p><a href="<%= t.sampleurl %>">サンプル</a></p>
		<p><%= t.comment %></p>
		<p><a href="/delete/<%= t.id%>">削除</a></p>
		<p><a href="/edit/<%= t.id%>">編集</a></p>
		</div>
	<% end %>
</body>
</html>
　　　　
　　　
